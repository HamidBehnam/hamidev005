import { LogoInformationDirective } from './logo-information.directive';

describe('LogoInformationDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoInformationDirective();
    expect(directive).toBeTruthy();
  });
});
